<div class="row mt-5 mb-4">
    <div class="col-12 text-center">
        <div class="row justify-content-center">
            {% for category in categories %}
            <div class="col-auto">
                <p class="tags"><a href="" class="">{{ category.name }}</a></p>
            </div>
            {% endfor %}
        </div>
    </div>
</div>